{
  "name": "fable-react-poc",
  "version": "0.1.0",
  "description": "Playing around with Fable",
  "scripts": {
    "pre-start": "npm install && dotnet tool restore",
    "start": "dotnet fable watch src -o dist --run vite",
    "preview": "dotnet fable watch src -o dist --run vite preview",
    "pre-build": "npm i --frozen-lockfile && dotnet tool restore",
    "build": "dotnet fable src -o dist && vite build"
  },
  "devDependencies": {
    "vite": "4.3.4",
    "vite-plugin-minify": "^1.5.2"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}